package br.com.fiap.hospital.agendamento.integracao.superficie;

/** A familia de valores hostis que se aplica a uma entrada, decidida pelo tipo dela. */
public enum Dimensao {
    UUID,
    ENUM,
    DATA,
    INTEIRO,
    TEXTO,
    CORPO
}

## 1. Gate estrutural da auditoria de requisitos (D3, D5, D9, D11)

- [x] 1.1 Criar `AuditoriaDeRequisitosTest` em `quality-gates/src/test`, sem acrescentar dependência ao POM, lendo `docs/02-especificacao-funcional.md` em UTF-8 com parser próprio e afirmando **exatamente 20 identificadores RF e 10 RNF**, contínuos de RF-01 a RF-20 e de RNF-01 a RNF-10, únicos e na ordem do documento. Verificar que o teste compila e que a asserção fica vermelha se o parser devolver lista vazia.
- [x] 1.2 Implementar no mesmo teste a execução real de `scripts/auditoria.sh` sobre a raiz do repositório, localizando o Bash nativo com a mesma estratégia de `RoteiroDeDemonstracaoTest`. Exigir código 0, cabeçalho `REQUISITO | STATUS | EVIDÊNCIA`, a frase que fixa a semântica de `OK` como evidência presente e ancorada, exatamente 30 linhas de requisito, três campos não vazios por linha, ordem idêntica à do documento e sumário coerente. Verificar vermelho agora, pela ausência do script.
- [x] 1.3 Implementar as verificações do catálogo segundo o D4 e o D5: cada identificador do documento tem **pelo menos um** par artefato + âncora e o conjunto do requisito é avaliado por conjunção; nenhuma entrada órfã; a âncora não é nem contém o identificador do requisito; a âncora não é apenas nome de comando, ferramenta ou execução; par repetido entre requisitos distintos exige declaração de compartilhamento; o total de pares confere com a tabela do D6, que tem **75**; RNF-04, RNF-06 e RNF-07 ancoram no piso, no gate agregado e no alvo de build, e não em citação de comando. Verificar cada regra com uma fixture sintética que a viola.
- [x] 1.4 Implementar as regras estruturais sobre o texto do script, no estilo de `RoteiroDeSmoke`, mas **distinguindo dado de código executável** (D8): antes de analisar, descartar comentários e o conteúdo literal das entradas do catálogo; sobre o que sobrar, exigir `set -Eeuo pipefail` como primeiro comando, presença do guarda `BASH_SOURCE`, raiz aceita por argumento, ausência de redirecionamento de escrita para caminho do repositório e **ausência de invocação** de `docker`, `mvn`, `psql`, `curl`, `pkill`, `killall` e `mktemp` — em início de comando, após separador do shell (`;`, `&&`, `||`, `|`, `&`, `(`, `{`, nova linha), dentro de `$(...)`, crase ou subshell, e por caminho absoluto. Verificar uma violação nomeada por regra e, como controle positivo, que os mesmos tokens em comentário e em valor do catálogo **não** produzem violação.
- [x] 1.5 Implementar os oito negativos sintéticos da tabela do D11 sobre cópias em diretório temporário validado, uma mutação por caso, executando o script com a raiz apontada para a cópia. Incluir o caso da **âncora obrigatória ausente num requisito multi-âncora**, que precisa reprovar o requisito inteiro mesmo com as demais âncoras dele passando. Verificar o código de saída exigido e a mensagem específica de cada caso, restaurando a cópia e reexecutando o positivo antes da mutação seguinte, sem tocar na árvore de trabalho.
- [x] 1.6 Implementar o controle de invocação em execução do D8: montar um diretório com executáveis sentinela chamados `docker`, `mvn`, `psql`, `curl`, `pkill`, `killall` e `mktemp`, cada um registrando a própria chamada num arquivo e saindo com erro, colocar esse diretório no **início do `PATH`** e executar `scripts/auditoria.sh` inteiro. Exigir código 0 e **nenhum sentinela chamado**. Verificar a sensibilidade do próprio controle com um script sintético que invoca um dos nomes, confirmando que o sentinela registra a chamada.

## 2. Roteiro `scripts/auditoria.sh` e a lacuna que ele revelou (D2, D3, D4, D5, D6, D6-A, D7, D8)

- [x] 2.1 Criar `scripts/auditoria.sh` com cabeçalho documentando propósito, uso, códigos de saída, a semântica de `OK` do D2 e a distinção explícita em relação à auditoria de execução do `mvn verify`. Incluir `set -Eeuo pipefail`, o guarda `BASH_SOURCE`, a raiz opcional por argumento com padrão no diretório acima do script e o `preflight` que recusa Bash incompatível antes de qualquer leitura. Verificar que carregar o arquivo por `source` não executa o fluxo.
- [x] 2.2 Implementar o inventário: extrair os identificadores das quatro tabelas de RF da §1 e da tabela de RNF da §2, preservando a ordem e o título da subseção de origem, e exigir a sequência fechada RF-01…RF-20 e RNF-01…RNF-10, contínua, única e na ordem. Recusar com código 4 quando a seção ou a tabela não for encontrada e com código 3 em lacuna, repetição, número fora da sequência ou entrada órfã, tudo sem executar o build nem alcançar o ambiente. Verificar as recusas com cópias mutiladas em diretório temporário.
- [x] 2.3 Fechar a lacuna **e a violação** do RNF-01 (D6-A). (a) Criar `SenhaForaDosLogsIT` em `agendamento-service/src/test/java/br/com/fiap/hospital/agendamento/integracao/`, com as anotações de `CadeiaDeSegurancaIT` — `@SpringBootTest(webEnvironment = RANDOM_PORT)` e `ContainerPostgres.registrarPropriedades` no `@DynamicPropertySource` —, para cair na mesma chave do cache de contexto. Semear o usuário com o hash BCrypt conhecido de `Senha@123`, anexar um `ListAppender<ILoggingEvent>` ao logger raiz em nível `DEBUG` guardando o nível anterior, e desanexar restaurando o nível em `@AfterEach`. Usar o **`java.net.http.HttpClient` do JDK** como cliente, e não `TestRestTemplate`: o cliente do JDK não escreve pelo Logback, então o appender observa o lado servidor sem precisar ignorar eventos genericamente. (b) Executar `POST /auth/login` com senha correta (200) e com senha errada (401) e exigir ausência da senha correta, da senha incorreta e do hash BCrypt nas mensagens formatadas, nos argumentos, na cadeia de `throwable` e nos dois corpos de resposta, mantendo a asserção de que **a captura não veio vazia**. **Não baixar o nível de log para esconder vazamento.** (c) Registrar a execução vermelha como evidência do defeito encontrado, com os loggers de origem nomeados. (d) Corrigir a causa sobrescrevendo `toString()` em `agendamento-service/src/main/java/br/com/fiap/hospital/agendamento/infrastructure/web/LoginRequest.java` para nunca incluir o valor de `senha`, por marcador fixo ou omissão do campo, sem tocar no contrato JSON, na validação nem na autenticação — **é o único arquivo de produção que esta change altera**. (e) Reexecutar `mvn -q -pl agendamento-service -am verify -Dit.test=SenhaForaDosLogsIT -Dsurefire.failIfNoSpecifiedTests=false -Dfailsafe.failIfNoSpecifiedTests=false` e exigir verde, registrando do log do Spring que nenhum contexto novo foi criado.
- [x] 2.4 Escrever o catálogo no topo do script com os **75 pares** da tabela do D6, agrupados pelos 30 requisitos, cada par com caminho completo do artefato e âncora literal. Antes de marcar a task, reabrir cada artefato citado e confirmar a âncora byte a byte, e reler o texto integral de cada RF/RNF em `docs/02` conferindo que o conjunto de pares cobre todas as cláusulas daquele requisito. Nenhuma âncora pode ser o identificador do requisito nem nome de comando isolado; par repetido entre requisitos exige declaração de compartilhamento.
- [x] 2.5 Implementar o verificador único do D4 — artefato existe, é legível e contém a âncora —, aplicado **por conjunção** ao conjunto de pares de cada requisito, o cruzamento do catálogo com o inventário nos dois sentidos e a montagem de **uma linha por requisito** no formato `REQUISITO | STATUS | EVIDÊNCIA`, com o campo de evidência enumerando todas as âncoras por um delimitador interno que não colida com o `|` das colunas, agrupada pelos títulos das subseções do documento e seguida do sumário de inventariados, aprovados e reprovados. Verificar que uma âncora faltante reprova o requisito inteiro, que uma reprovação isolada não interrompe o relatório e que o código de saída vai a 1.
- [x] 2.6 Executar `bash scripts/auditoria.sh` na raiz e registrar a saída completa: 30 linhas de requisito, 75 âncoras conferidas, zero reprovados, código 0. Verificar as tasks 1.2 a 1.6 e a 2.3 verdes e conferir, com `git status --porcelain`, que a execução não criou nem alterou arquivo algum.

## 3. Gate e conteúdo do roteiro de demonstração (D9, D12)

- [x] 3.1 Criar `RoteiroDeApresentacaoTest` em `quality-gates/src/test` com as verificações do roteiro: soma das durações dos blocos cronometrados entre 5 e 8 minutos inclusive; cada bloco cronometrado com duração própria; seção de pré-demonstração presente e explicitamente fora da soma; as cinco superfícies com os endereços reais (`:8081/swagger-ui.html`, `:8083/graphiql`, `:15672`, `:8025` e os logs correlacionados); credenciais idênticas às de `docs/02` §5; comandos existentes no `Makefile` ou em `scripts/`; nenhum comando que remova volumes ou apague dados; uma alternativa curta por bloco. Verificar vermelho pela ausência de `docs/roteiro-demo.md`.
- [x] 3.2 Acrescentar negativos sintéticos ao `RoteiroDeApresentacaoTest`, um por desvio: bloco sem duração, soma abaixo de 5 minutos, soma acima de 8 minutos, pré-demonstração contada na soma, superfície ausente, credencial divergente, comando inexistente, comando destrutivo de volumes e bloco sem alternativa. Verificar uma violação nomeada por caso.
- [x] 3.3 Escrever `docs/roteiro-demo.md` com a seção de pré-demonstração e os seis blocos cronometrados do D12, conferindo cada comando, endereço e credencial contra `docker-compose.yml`, `Makefile`, `scripts/demo.sh`, `.env.example` e `docs/02` §5, e incluindo a alternativa curta de cada bloco. Verificar a task 3.1 verde e a soma dentro da janela.
- [x] 3.4 Ensaiar os blocos cronometrados com o ambiente já saudável, subido pelo `make demo` idempotente e **sem** `docker compose down -v` nem remoção de volumes, cronometrando cada bloco e ajustando as durações declaradas ao que realmente levou. Registrar o tempo total medido e confirmar que ele continua entre 5 e 8 minutos, sem cortar conteúdo para caber.

## 4. Gate de medição: execuções reais e coleta dos números (D2, D10)

- [x] 4.1 Executar uma única vez `mvn -q clean verify` na raiz, sem filtro, como **gate de medição do M14** — é este o rótulo que o relatório vai citar como origem dos seus números. Registrar build verde, casos por módulo, total de casos, zero pulados, auditoria de execução e infraestrutura aprovadas e as coberturas global, `domain` e `application` com os valores exatos do relatório agregado. Confirmar que `RelatorioTecnicoTest` ainda não existe, de modo que nenhum gate exija artefato ausente nesta execução.
- [x] 4.2 Executar `scripts/smoke-test.sh` e registrar código de saída 0, ausência de recurso órfão e o `correlationId` observado nos logs dos três serviços.
- [x] 4.3 Executar `bash scripts/auditoria.sh` com o ambiente completo no ar e, em seguida, com ele desligado — sem apagar volumes. Registrar que as duas saídas são idênticas e que nenhum container, volume ou arquivo foi criado, alterado ou removido, como evidência do Scenario de ausência de efeito.
- [x] 4.4 Consolidar num único bloco de registro todos os números coletados em 2.5, 3.4, 4.1, 4.2 e 4.3, cada um com o comando que o produziu. Nenhum número entra no relatório sem constar deste bloco.

## 5. Relatório técnico e README (D2, D9, D10, D13)

- [x] 5.1 Criar `RelatorioTecnicoTest` em `quality-gates/src/test`: as oito seções obrigatórias do D10 presentes uma única vez e na ordem; tabela de números com coluna de origem preenchida em todas as linhas; links locais existentes e dentro do repositório; nenhum `.docx`, `.doc`, `.odt` ou `.pdf` versionado por esta entrega. Acrescentar negativos sintéticos para seção ausente, renomeada, duplicada e fora de ordem, linha de número sem origem e link quebrado. Verificar vermelho pela ausência de `docs/relatorio-tecnico.md`.
- [x] 5.2 Escrever `docs/relatorio-tecnico.md` com as oito seções em português e tom técnico, apoiado nos artefatos existentes — os sete ADRs, `docs/01`, `docs/03`, o `CHANGELOG.md` e as specs promovidas —, sem decisão nova e sem releitura retrospectiva. Verificar a task 5.1 verde nas seções, na ordem e nos links.
- [x] 5.3 Preencher a seção de testes com a tabela de números do bloco da task 4.4, cada linha nomeando o comando de origem, e declarar em texto que esses números vêm do **gate de medição do M14** da task 4.1 e não são reescritos por execuções posteriores. Declarar também que a auditoria de requisitos é estática — `OK` é evidência presente e ancorada, com o total de âncoras conferidas — enquanto build, smoke e ambiente são as execuções que comprovam comportamento. Qualquer número sem origem registrada é removido em vez de estimado.
- [x] 5.4 Escrever a seção de limites declarando a entrega ao-menos-uma-vez da publicação e da notificação, as omissões já registradas na 0.2.0, o que ficou fora do escopo pelo charter, a ambiguidade do enunciado resolvida na ADR-004, o alcance da auditoria estática e o **limite da prova de log do RNF-01** do D6-A — ela cobre o que a JVM da suíte escreve durante os dois logins, em `DEBUG` ou acima, e não alcança bibliotecas fora do Logback nem níveis ligados só em produção. Verificar que nenhuma garantia declarada é mais forte do que a implementada.
- [x] 5.5 Atualizar `README.md` apenas com o apontamento para `scripts/auditoria.sh`, `docs/relatorio-tecnico.md` e `docs/roteiro-demo.md` e com a distinção entre auditoria de execução e auditoria de requisitos. Verificar `DocumentacaoFinalTest` ainda verde e conferir que nenhum outro documento do repositório foi tocado, **excluída desta afirmação a própria change em `openspec/changes/finalize-audit-report/`**.

## 6. Gate final e verificação (D10)

- [x] 6.1 Executar `openspec validate finalize-audit-report --strict` e conferir 4 Requirements, 23 Scenarios e 4/4 artefatos. Preencher a matriz abaixo com um método automatizado ou evidência de execução por Scenario; falhar se não houver exatamente 23 linhas mapeadas.
- [x] 6.2 Executar a suíte dirigida `mvn -q -pl quality-gates -am test -Dtest='AuditoriaDeRequisitosTest,RoteiroDeApresentacaoTest,RelatorioTecnicoTest' -Dsurefire.failIfNoSpecifiedTests=false`. Registrar casos, zero falhas, erros e pulados, e conferir que os relatórios são novos. O `SenhaForaDosLogsIT` não entra aqui: é integração, já foi exercitado na task 2.3 e volta a rodar nas duas execuções completas.
- [x] 6.3 Provar sensibilidade sobre os **arquivos reais**, sem repetir os negativos já automatizados na task 1.5: apagar uma âncora real do repositório e, em seguida, substituir uma evidência real pela menção textual do identificador. Para cada uma, registrar vermelho com a mensagem esperada, restaurar o arquivo byte a byte e reexecutar o teste até verde antes da mutação seguinte.
- [x] 6.4 Executar `mvn -q clean verify` na raiz uma segunda e última vez, como **gate final**, agora com os três gates novos e o relatório completo. Registrar build verde, casos por módulo, zero pulados e coberturas acima dos pisos, **como evidência separada no PR**. A diferença de contagem em relação ao gate de medição é esperada e atribuível aos casos de `RelatorioTecnicoTest`: confirmar que ela se explica inteiramente por eles e registrar a explicação. Cobertura abaixo de piso, suíte vermelha, teste pulado ou divergência não atribuível a esses casos **interrompem a entrega para investigação**. Não editar o relatório nesta task: nenhuma alteração da árvore ocorre depois do gate final.
- [x] 6.5 Conferir com `git diff` de leitura que o **único** arquivo de produção criado ou alterado é `agendamento-service/src/main/java/br/com/fiap/hospital/agendamento/infrastructure/web/LoginRequest.java`, e que nele muda apenas o `toString()` — sem alteração de componentes, anotações de validação ou contrato JSON. Confirmar que nenhum outro arquivo sob `src/main` de qualquer módulo, inclusive o `quality-gates`, foi tocado, e que os demais artefatos são exatamente `scripts/auditoria.sh`, `docs/relatorio-tecnico.md`, `docs/roteiro-demo.md`, os **quatro** arquivos de teste novos — `quality-gates/src/test/java/br/com/fiap/hospital/qualidade/AuditoriaDeRequisitosTest.java`, `.../RoteiroDeApresentacaoTest.java`, `.../RelatorioTecnicoTest.java` e `agendamento-service/src/test/java/br/com/fiap/hospital/agendamento/integracao/SenhaForaDosLogsIT.java` —, `README.md` e a própria change em `openspec/changes/finalize-audit-report/`. Se a alternativa estrutural do D6-A tiver sido acionada, o quarto arquivo é a guarda no `quality-gates/src/test` em vez do IT, e a substituição fica registrada no PR. Confirmar que `CHANGELOG.md`, `pom.xml`, os sete ADRs, `docs/00` a `docs/05`, `docker-compose.yml`, `Makefile`, `scripts/demo.sh`, `scripts/smoke-test.sh` e o pacote Postman permanecem byte a byte iguais.
- [x] 6.6 Depois do push autorizado pelo Gabriel, clonar a feature num diretório temporário validado, usar repositório Maven local vazio e executar `mvn -q -Dmaven.repo.local=<repo> clean verify` e `bash scripts/auditoria.sh`. Registrar SHA, código 0 nos dois, contagens, zero pulados e coberturas; remover o temporário por trap seguro.
- [x] 6.7 Montar o corpo do PR com: matriz dos 23 Scenarios, saída completa da auditoria com os 75 pares conferidos, contagens e coberturas do **gate de medição** e do **gate final** lado a lado, com a diferença de contagem explicada por `RelatorioTecnicoTest`, resultado do smoke, as duas mutações de sensibilidade, clone limpo, a **violação do RNF-01 que a auditoria encontrou** — com os loggers de origem, a execução vermelha antes da correção e a verde depois —, a exceção nominal única em `LoginRequest.java` e a confirmação de que nenhum outro arquivo de produção mudou, decisões D1 a D13 e riscos residuais. Declarar que os números do relatório são os do gate de medição e não foram reescritos, que `OK` na auditoria significa evidência presente e ancorada, e que abertura de `release/1.0.0`, versão, `CHANGELOG.md`, merge e tag ficam para o Gabriel, depois do merge do M14.
- [x] 6.8 Após aprovação do PR e antes do merge, executar `/opsx:archive finalize-audit-report` na mesma feature branch. Confirmar a change fora da área ativa, presente no archive datado, delta promovido em `operacao-do-ambiente` sem alterar Requirements anteriores, nenhuma outra capability tocada, `openspec validate --all --strict` verde e `openspec list` sem change ativa. Conferir no diff publicado que script, documentos, gates, archive e spec promovida estão na mesma branch antes de liberar o merge `--no-ff`.

### Matriz Scenario → evidência

| # | Requirement | Scenario | Evidência planejada |
|---:|---|---|---|
| 1 | Auditoria executável | Inventário completo é percorrido | `AuditoriaDeRequisitosTest#inventarioCobreOsTrintaRequisitos` |
| 2 | Auditoria executável | Inventário fora da sequência fechada é recusado | `AuditoriaDeRequisitosTest#sequenciaIncompletaEhRecusada` |
| 3 | Auditoria executável | Relatório usa o formato de três campos | `AuditoriaDeRequisitosTest#relatorioTemCabecalhoETresCampos` |
| 4 | Auditoria executável | Repositório íntegro termina com código zero | `AuditoriaDeRequisitosTest#execucaoRealTerminaVerde` + execução 2.5 |
| 5 | Auditoria executável | Requisito sem evidência é recusado | `AuditoriaDeRequisitosTest#requisitoSemEvidenciaEhRecusado` |
| 6 | Auditoria executável | Identificador duplicado é recusado | `AuditoriaDeRequisitosTest#identificadorDuplicadoEhRecusado` |
| 7 | Auditoria executável | Evidência inexistente é recusada | `AuditoriaDeRequisitosTest#artefatoAusenteEhRecusado` |
| 8 | Auditoria executável | Auditoria não altera o ambiente | `AuditoriaDeRequisitosTest#roteiroNaoAlcancaAmbienteNemEscreve` + `#nenhumSentinelaDePathEhInvocado` + execução 4.3 |
| 9 | Evidência ancorada | Evidência aponta para artefato e elemento | `AuditoriaDeRequisitosTest#catalogoApontaArtefatoEAncora` + `#conjuntoDeAncorasCobreAsClausulas` |
| 10 | Evidência ancorada | Âncora ausente do artefato existente é recusada | `AuditoriaDeRequisitosTest#ancoraAusenteReprovaORequisito` + `#ancoraFaltanteReprovaRequisitoMultiAncora` + mutação 6.3 |
| 11 | Evidência ancorada | Menção textual do identificador não aprova o requisito | `AuditoriaDeRequisitosTest#mencaoTextualNaoAprovaORequisito` + mutação 6.3 |
| 12 | Evidência ancorada | Evidência compartilhada sem declaração é recusada | `AuditoriaDeRequisitosTest#parRepetidoExigeCompartilhamentoDeclarado` |
| 13 | Evidência ancorada | Requisito garantido por execução aponta para quem a exige | `AuditoriaDeRequisitosTest#requisitosDeExecucaoAncoramNoGate` |
| 14 | Relatório técnico | Seções obrigatórias estão presentes | `RelatorioTecnicoTest#relatorioTemAsOitoSecoesNaOrdem` |
| 15 | Relatório técnico | Cada número nomeia sua origem | `RelatorioTecnicoTest#numerosDoRelatorioNomeiamOrigem` + `#secaoDeTestesCitaOGateDeMedicao` |
| 16 | Relatório técnico | Limites e cortes são declarados | `RelatorioTecnicoTest#relatorioDeclaraLimitesEAmbiguidades` |
| 17 | Relatório técnico | Links locais do relatório são válidos | `RelatorioTecnicoTest#linksLocaisDoRelatorioExistem` |
| 18 | Relatório técnico | Relatório permanece um documento de texto versionado | `RelatorioTecnicoTest#nenhumBinarioDeEscritorioEhVersionado` |
| 19 | Roteiro de demonstração | Duração total cabe na janela de 5 a 8 minutos | `RoteiroDeApresentacaoTest#duracaoCronometradaFicaEntreCincoEOito` + ensaio 3.4 |
| 20 | Roteiro de demonstração | As cinco superfícies obrigatórias aparecem com endereço real | `RoteiroDeApresentacaoTest#roteiroCobreAsCincoSuperficies` |
| 21 | Roteiro de demonstração | Comandos e credenciais conferem com os artefatos reais | `RoteiroDeApresentacaoTest#comandosECredenciaisConferemComOsArtefatos` |
| 22 | Roteiro de demonstração | Correlação é demonstrada por um único identificador | `RoteiroDeApresentacaoTest#blocoDeLogsExigeCorrelacaoUnica` + smoke 4.2 |
| 23 | Roteiro de demonstração | Falha de uma interface tem alternativa curta | `RoteiroDeApresentacaoTest#cadaBlocoTemAlternativaCurta` |
